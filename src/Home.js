import React from "react";
import { Button } from "react-bootstrap";

function Home() {
    return (
        <div>
            <h1>Home</h1>
            <Button variant="primary">Submit</Button>
        </div>
    );
}

export default Home;
